#Not Complete, obviously..
ipmi power on
systemctl status glusterd
   if not up... systemctl start glusterd
ip addr show
gluster peer status
gluster volume status engine
hosted-engine --vm-start
hosted-engine --vm-status
hosted-engine --set-maintenance --mode=none
